generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum Role {
  ADMIN
  USER
}

model Profile {
  id        String   @id @db.Uuid
  role      Role     @default(USER)

  @@map("profile")
}
